<!--<script>
  import { stores } from '@sapper/app';

  const { page } = stores();
  // register page changes in Google Analytics
  $: {
    if (typeof gtag !== 'undefined') {
      gtag('config', 'UA-54909104-11', {
        page_path: $page.path
      });
    }
  }
</script>-->
